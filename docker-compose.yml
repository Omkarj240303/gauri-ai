services:
  backend:
    image: gauri-ai-backend
    env_file:
      - .env
    expose:
      - "8000"

  frontend:
    image: gauri-ai-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

      